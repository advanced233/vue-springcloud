server:
  port: 8103

spring:
  application:
    name: campus-food-order-service
  datasource:
    url: jdbc:mysql://localhost:3306/campus_food?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: Wogua123456789
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
